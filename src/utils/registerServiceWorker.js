// Original content from your previous service worker file remains unchanged
export function register(config) {
    // Register the service worker, etc.
  }
  
  export function unregister() {
    // Unregister logic here
  }
  